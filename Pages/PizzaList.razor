@if (Pizzas is null || !Pizzas.Any())
{
    <div>Loading...</div>
}
else
{
    <h1>@Title</h1>
    @foreach (var pizza in Pizzas)
    {
        <PizzaItem Pizza="@pizza" ButtonClass="@ButtonClass" ButtonTitle="@ButtonTitle" Selected="@Selected"/>
    }
}